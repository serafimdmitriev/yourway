<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@color/colorWarning">

    <ImageView android:id="@+id/imgIconWarning"
        android:gravity="center_vertical"
        android:layout_width="17dp"
        android:layout_height="17dp"
        android:background="@drawable/icon_warning"
        android:layout_marginLeft="17dp"
        android:layout_marginStart="17dp"
        android:layout_marginRight="17dp"
        android:layout_marginEnd="17dp"
        android:layout_marginTop="12dp"
        android:layout_marginBottom="8dp" />

    <TextView
        android:text="Be careful, something somewhere somewhen goes wrong wrong wrong!"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:layout_marginRight="15dp"
        android:layout_marginEnd="15dp"
        android:layout_marginTop="9dp"
        android:layout_marginBottom="9dp"
        android:layout_toRightOf="@+id/imgIconWarning"
        android:layout_toEndOf="@+id/imgIconWarning"
        android:id="@+id/textTextWarning"
        android:textSize="14sp"
        android:textColor="@color/colorDarkGray" />

</RelativeLayout>